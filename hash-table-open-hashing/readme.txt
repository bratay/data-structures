This is a hash table with open hashing with a terminal based menu the controls are as follows:

1. Insert: Inserts an element into the hash table
2. Delete: Deletes an element from the hash table 
3. Find: looks for a given element prints the index it's located at if found
4. Print: Prints all elements of the table
5. exit: Exits the program
